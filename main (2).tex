\documentclass[12pt,a4paper]{article}

% ============================================================
% PACKAGES
% ============================================================
\usepackage[utf8]{inputenc}
\usepackage[T5]{fontenc}
\usepackage[vietnamese]{babel}
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{amsmath}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows, positioning}

% ============================================================
% THIẾT LẬP TIKZ STYLES
% ============================================================
\tikzstyle{layer} = [rectangle, rounded corners, minimum width=3cm, minimum height=1.2cm, text centered, draw=blue!80, fill=blue!5, text width=2.8cm, font=\small]
\tikzstyle{dropout} = [rectangle, rounded corners, minimum width=3cm, minimum height=1.2cm, text centered, draw=orange!80, fill=orange!5, text width=2.8cm, font=\small]
\tikzstyle{dense} = [rectangle, rounded corners, minimum width=3cm, minimum height=1.2cm, text centered, draw=green!70!black, fill=green!5, text width=2.8cm, font=\small]
\tikzstyle{arrow} = [thick,->,>=stealth]

% ============================================================
% THIẾT LẬP HYPERREF & LISTINGS
% ============================================================
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    urlcolor=blue,
}

\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    backgroundcolor=\color{gray!10},
}

\pagestyle{fancy}
\fancyhf{}
\rhead{Nhóm 11 --- Báo cáo Mô hình Học máy}
\lhead{Môn Nhập môn Học máy}
\rfoot{\thepage}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}

% ============================================================
% BẮT ĐẦU TÀI LIỆU
% ============================================================
\begin{document}

% ── TRANG BÌA ──────────────────────────────────────────────
\begin{titlepage}
    \centering
    \vspace*{1cm}
    {\Large \textbf{ĐẠI HỌC KHOA HỌC TỰ NHIÊN TP.HCM}}\\[0.3cm]
    {\large Khoa Công nghệ Thông tin}\\[2cm]

    {\Huge \textbf{BÁO CÁO MÔ HÌNH HỌC MÁY}}\\[0.5cm]
    {\large Xây dựng và Triển khai Hệ thống Học máy Ứng dụng}\\[2cm]

    {\LARGE \textbf{Dự đoán Chất lượng Không khí Việt Nam}}\\[0.3cm]
    {\large và Cảnh báo Nguy cơ Mắc Bệnh Hô hấp}\\[2cm]

    \begin{tabular}{cll}
        \toprule
        \textbf{STT} & \textbf{MSSV} & \textbf{Họ và tên} \\
        \midrule
        1 & 23120062 & Trần Kim Ngọc \\
        2 & 23120063 & Nguyễn Thành Nguyên \\
        3 & 23120084 & Nguyễn Mạnh Thắng \\
        4 & 23120059 & Trần Đình Luân \\
        5 & 23120064 & Nguyễn Thiện Nhân \\
        \bottomrule
    \end{tabular}\\[2cm]

    {\large Nhóm 11}
\end{titlepage}

\tableofcontents
\vspace{0.5cm}
\noindent\textbf{Repository GitHub:} \href{https://github.com/kimngoc280105/aqi-vietnam}{https://github.com/kimngoc280105/aqi-vietnam}
\newpage

% ============================================================
% 1. GIỚI THIỆU BÀI TOÁN
% ============================================================
\section{Giới thiệu bài toán}

Ô nhiễm môi trường không khí tại Việt Nam, đặc biệt là nồng độ bụi mịn $PM_{2.5}$ tại các đô thị lớn như Hà Nội, TP.HCM và Đà Nẵng đang là vấn đề lớn đe dọa đến sức khỏe cộng đồng. Nhóm hướng đến giải quyết bài toán có ý nghĩa thực tiễn cao: \textbf{hồi quy chuỗi thời gian để dự báo trực tiếp nồng độ bụi mịn $PM_{2.5}$ trong không khí cho 24 giờ tới ($t+1$ đến $t+24$)}. Các mức cảnh báo sức khỏe và ma trận nhầm lẫn chỉ được xây dựng sau đó bằng cách ánh xạ giá trị $PM_{2.5}$ thực tế/dự báo sang các khoảng ngưỡng rủi ro. Việc dự báo trước 24 giờ là cực kỳ quan trọng đối với các nhóm đối tượng nhạy cảm như người mắc bệnh hô hấp mãn tính (hen suyễn, viêm phế quản, COPD) để họ có đủ thời gian chuẩn bị các biện pháp y tế và lập kế hoạch hoạt động an toàn trước một ngày.

Trong các thiết kế ban đầu của hệ thống, chỉ số chất lượng không khí tổng hợp (AQI) được cân nhắc làm biến mục tiêu dự đoán. Tuy nhiên, qua phân tích khám phá dữ liệu (EDA), nhóm đã thực hiện bước chuyển đổi chiến lược (\textit{Target Pivot}) sang dự đoán trực tiếp nồng độ vật chất tuyệt đối $PM_{2.5}$ vì các lý do sau:
\begin{enumerate}
    \item \textbf{Hiện tượng che khuất (Masking effect):} Chỉ số AQI tổng hợp được tính bằng hàm $Max$ nồng độ của nhiều loại khí ($PM_{2.5}, O_3, NO_2, SO_2, CO$). Khi một loại khí giảm đột biến (ví dụ $O_3$ vào ban đêm) và bụi mịn $PM_{2.5}$ tăng cao, AQI tổng thể có thể không phản ánh được mức tăng cục bộ nguy hiểm của bụi mịn.
    \item \textbf{Độ mượt toán học:} $PM_{2.5}$ tuân theo tự tương quan chuỗi thời gian ($Autocorrelation$) liên tục và bám sát các quy luật vật lý (khuếch tán, phát tán) tốt hơn chuỗi AQI vốn mang tính giật cục do các phép làm mượt 24 giờ.
    \item \textbf{Tính thực tế y khoa:} $PM_{2.5}$ là tác nhân nguy cơ chính xâm nhập sâu vào phế nang phổi và ngấm vào máu. Dự đoán trực tiếp $PM_{2.5}$ giúp nâng cao tính tường minh (Explainable AI) và độ tin cậy trong các cảnh báo sức khỏe y tế.
\end{enumerate}

% ============================================================
% 2. TỔNG QUAN DỮ LIỆU ĐẦU VÀO
% ============================================================
\section{Tổng quan dữ liệu đầu vào}

\subsection{Dữ liệu huấn luyện và Tỷ lệ chia dữ liệu}

Dữ liệu thực nghiệm bao gồm 98,187 mẫu thu thập theo giờ từ ngày \textbf{05/08/2022} đến \textbf{29/04/2026} cho ba thành phố Hà Nội, TP.HCM và Đà Nẵng. Vì đặc thù của dữ liệu chuỗi thời gian chứa tính tự tương quan cao, việc chia dữ liệu ngẫu nhiên sẽ dẫn đến hiện tượng rò rỉ thông tin tương lai (\textit{Data Leakage} hay \textit{Look-ahead Bias}). 

Do đó, nhóm sử dụng phương pháp chia dữ liệu theo trình tự thời gian (\textbf{Time-series Split}) với tỷ lệ \textbf{70/15/15}:
\begin{itemize}
    \item \textbf{Tập Huấn luyện (Train) - 70\%}: Dùng để tối ưu hóa trọng số của các mô hình học máy.
    \item \textbf{Tập Kiểm định (Validation) - 15\%}: Dùng để điều chỉnh siêu tham số và kích hoạt các hàm dừng sớm (\textit{Early Stopping}) nhằm chống học tủ.
    \item \textbf{Tập Kiểm thử (Test) - 15\%}: Giữ độc lập hoàn toàn để đánh giá hiệu năng cuối cùng của mô hình trên dữ liệu chưa từng thấy.
\end{itemize}

\subsection{Các bước Tiền xử lý Dữ liệu}

Trước khi đưa dữ liệu vào mô hình, các bước chuẩn bị dữ liệu kỹ thuật sau đã được áp dụng:
\begin{enumerate}
    \item \textbf{Nội suy dữ liệu khuyết thiếu (Imputation):} Sử dụng nội suy tuyến tính (\textit{Linear Interpolation}) kết hợp \textit{Forward Fill} và \textit{Backward Fill} theo từng phân nhóm thành phố độc lập. Tỷ lệ khuyết thiếu thực tế ban đầu cực kỳ thấp ($<0.08\%$) ở các biến trễ thời gian và đạt mức $0\%$ sạch hoàn toàn sau xử lý.
    \item \textbf{Xử lý nhiễu và bất thường (Anomalies):} Phát hiện các giá trị âm ở nồng độ Ozone ($O_3$) do lỗi cảm biến và chuyển thành giá trị trống ($NaN$) để tiến hành nội suy.
    \item \textbf{Giải quyết đa cộng tuyến (Multicollinearity):} Từ ma trận tương quan, biến không gian chỉ số nhà máy trong bán kính 10km ($factories_{10km}$) có tương quan hoàn hảo $r=1.00$ với biến $factories_{5km}$, gây hiện tượng đa cộng tuyến nghiêm trọng. Nhóm quyết định loại bỏ cột $factories_{10km}$, chỉ giữ lại $factories_{2km}$ và $factories_{5km}$.
    \item \textbf{Xử lý ngoại lệ (Outliers):} Áp dụng quy tắc Tukey's Fence mở rộng ($Q3 + 3 \times IQR$). Nhóm quyết định \textbf{giữ lại toàn bộ ngoại lệ} vì chúng phản ánh các đợt ô nhiễm cực đoan có thật trong thực tế (đốt rơm rạ, nghịch nhiệt bức xạ) - những thời điểm mà hệ thống cảnh báo cần hoạt động chính xác nhất.
    \item \textbf{Trích xuất đặc trưng (Feature Engineering):}
    \begin{itemize}
        \item \textit{Đặc trưng trễ (Lag features):} Tạo các cột $PM_{2.5}$ trễ 1h, 3h, 6h, 12h, 24h để nắm bắt xu hướng ngắn hạn.
        \item \textit{Đặc trưng cửa sổ trượt (Rolling features):} Tính trung bình trượt nồng độ $PM_{2.5}$ trong 6h, 24h và 72h để làm mịn nhiễu.
        \item \textit{Mã hóa biến phân loại:} Thực hiện One-Hot Encoding cho các biến địa lý (\texttt{city}) và thời tiết mùa (\texttt{season}).
    \end{itemize}
    \item \textbf{Chuẩn hóa đặc trưng (Scaling):} Sử dụng \texttt{StandardScaler} (Z-score normalization) cho các thuật toán Linear Regression/XGBoost, và \texttt{MinMaxScaler} trong khoảng $[0, 1]$ cho mạng LSTM để hỗ trợ sự hội tụ của hàm kích hoạt phi tuyến. Bộ chuẩn hóa chỉ được khớp ($fit$) trên tập Train và biến đổi ($transform$) trên tập Val/Test nhằm tránh rò rỉ dữ liệu.
\end{enumerate}

% ============================================================
% 3. LỰA CHỌN MÔ HÌNH VÀ KIẾN TRÚC
% ============================================================
\section{Lựa chọn Mô hình \& Kiến trúc}

Nhóm nghiên cứu và phát triển 3 lớp mô hình đại diện cho 3 trường phái tiếp cận:

\begin{enumerate}
    \item \textbf{Mô hình Baseline (SARIMAX):} Lớp mô hình thống kê tuyến tính chuỗi thời gian cổ điển có kết hợp ngoại sinh (nhiệt độ, độ ẩm, tốc độ gió, áp suất). Được dùng làm mốc so sánh cơ sở.
    \item \textbf{Mô hình Học máy nâng cao (XGBoost Regressor):} Thuật toán học máy dựa trên cây quyết định nâng cao cực kỳ mạnh mẽ cho dữ liệu dạng bảng. XGBoost có khả năng học các mối quan hệ phi tuyến tính phức tạp và không nhạy cảm với phân phối lệch của bụi mịn.
    \item \textbf{Mô hình Học sâu tuần tự (LSTM):} Mạng nơ-ron hồi quy có cấu trúc cổng nhớ đặc biệt, thiết kế chuyên biệt để ghi nhớ các phụ thuộc dài hạn trong chuỗi thời gian.
\end{enumerate}

\subsection{Kiến trúc cấu trúc của Mô hình Thống kê SARIMAX}

Mô hình SARIMAX(1,0,1)(1,0,0,24) biểu diễn mối quan hệ tuyến tính động của nồng độ bụi mịn dưới dạng phương trình toán học:
\begin{equation}
    \Phi_1(L) \tilde{\Phi}_1(L^{24}) (1-L) (1-L^{24}) y_t = \Theta_1(L) \tilde{\Theta}_1(L^{24}) \varepsilon_t + \beta^T X_t
\end{equation}
Trong đó:
\begin{itemize}
    \item $y_t$ là nồng độ bụi mịn $PM_{2.5}$ tại thời điểm $t$.
    \item $L$ là toán tử trễ ($Lag$ operator) định nghĩa bởi $L^k y_t = y_{t-k}$.
    \item $\Phi_1(L) = 1 - \phi_1 L$ đại diện cho thành phần tự hồi quy phi mùa vụ bậc 1 (Non-seasonal AR(1)).
    \item $\tilde{\Phi}_1(L^{24}) = 1 - \tilde{\phi}_1 L^{24}$ đại diện cho thành phần tự hồi quy mùa vụ bậc 1 chu kỳ 24h (Seasonal AR(1)).
    \item $\Theta_1(L) = 1 + \theta_1 L$ đại diện cho thành phần trung bình trượt phi mùa vụ bậc 1 (Non-seasonal MA(1)).
    \item $\tilde{\Theta}_1(L^{24}) = 1 + \tilde{\theta}_1 L^{24}$ đại diện cho thành phần trung bình trượt mùa vụ bậc 1 chu kỳ 24h (Seasonal MA(1)).
    \item $(1-L)$ và $(1-L^{24})$ là các toán tử lấy hiệu phân bậc 1 phi mùa vụ và mùa vụ nhằm biến đổi chuỗi thời gian về trạng thái dừng.
    \item $X_t$ là vector các biến khí tượng ngoại sinh tại thời điểm $t$ gồm: $[\text{nhiệt độ}, \text{độ ẩm}, \text{tốc độ gió}, \text{áp suất}]$.
    \item $\beta = [\beta_1, \beta_2, \beta_3, \beta_4]^T$ là vector trọng số của các biến ngoại sinh.
    \item $\varepsilon_t$ là nhiễu trắng tuân theo phân phối chuẩn $N(0, \sigma^2)$.
\end{itemize}
Tổng số tham số ước lượng của mô hình là \textbf{9 tham số} (gồm $\phi_1, \tilde{\phi}_1, \theta_1, \tilde{\theta}_1, \beta_1, \beta_2, \beta_3, \beta_4, \sigma^2$).

\subsection{Kiến trúc cấu trúc của Mô hình Học máy XGBoost}

Mô hình XGBoost (eXtreme Gradient Boosting) hoạt động theo cơ chế học tăng cường độ dốc (\textit{Gradient Boosting}), trong đó cấu trúc là sự kết hợp của $M = 500$ cây quyết định hồi quy yếu (\textit{Regression Trees / CART}):
\begin{equation}
    \hat{y}_t = \sum_{m=1}^{500} f_m(X_t), \quad f_m \in \mathcal{F}
\end{equation}
Trong đó:
\begin{itemize}
    \item $X_t$ là vector đặc trưng đầu vào tại thời điểm $t$, gồm 26 đặc trưng khí tượng, không gian, thời gian và trễ.
    \item $f_m(X_t)$ là cây quyết định thứ $m$ được huấn luyện để dự báo phần dư sai số (\textit{residual}) của tổng các cây trước đó.
    \item Mỗi cây quyết định có độ sâu tối đa là $6$ lớp (\texttt{max\_depth = 6}), tương đương số lượng nút lá tối đa là $2^6 = 64$ nút lá trên mỗi cây.
    \item Hàm mục tiêu tối ưu hóa tích hợp cơ chế phạt Regularization để chống quá khớp:
    \begin{equation}
        \mathcal{L}^{(m)} = \sum_{i} l(y_i, \hat{y}_i^{(m-1)} + f_m(X_i)) + \left( \gamma T_m + \frac{1}{2} \lambda \sum_{j=1}^{T_m} w_j^2 \right)
    \end{equation}
    với $T_m$ là số nút lá của cây thứ $m$, $w_j$ là trọng số nút lá thứ $j$, $\gamma$ và $\lambda$ là các hệ số phạt nhằm kiểm soát độ phức tạp cấu trúc cây.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/xgboost_feature_importance.png}
    \caption{Top 15 đặc trưng quan trọng nhất của mô hình XGBoost trên bài toán dự báo $PM_{2.5}$ trước 24 giờ}
    \label{fig:xgb-feature-importance}
\end{figure}

\subsection{Kiến trúc chi tiết của Mô hình Học sâu LSTM}

Kiến trúc mạng LSTM được phát triển sử dụng phương pháp dự báo đa bước đầu ra vector (\textit{Vector-Output Multi-step Forecasting}), cho phép dự đoán đồng thời nồng độ bụi mịn cho cả 24 giờ tiếp theo:
\begin{itemize}
    \item \textbf{Lớp đầu vào (Input Layer):} Nhận vào chuỗi thời gian có kích thước $(Batch\_size, 48, 6)$, tương ứng cửa sổ quan sát trễ 48 giờ trước đó (\texttt{SEQ\_LEN} = 48) của 6 đặc trưng chính gồm: $[PM_{2.5}, temp, humidity, wind\_speed, pressure, PM_{10}]$.
    \item \textbf{Lớp LSTM thứ nhất:} Gồm 64 đơn vị cổng ẩn (\textit{units}), thiết lập \texttt{return\_sequences=True} để truyền tiếp chuỗi đầu ra sang lớp tiếp theo. Hàm kích hoạt mặc định là $tanh$ và hàm kích hoạt cổng là $sigmoid$. Số lượng tham số là: $4 \times ((6 + 64) \times 64 + 64) = 18,176$.
    \item \textbf{Lớp Dropout thứ nhất:} Tỷ lệ bỏ qua 20\% ($0.2$) nhằm chống quá khớp.
    \item \textbf{Lớp LSTM thứ hai:} Gồm 32 đơn vị cổng ẩn, thiết lập \texttt{return\_sequences=False} để nén chuỗi thời gian thành một vector đặc trưng duy nhất. Số lượng tham số là: $4 \times ((64 + 32) \times 32 + 32) = 12,416$.
    \item \textbf{Lớp Dropout thứ hai:} Tỷ lệ bỏ qua 20\% ($0.2$).
    \item \textbf{Lớp Dense thứ nhất (Fully Connected):} Gồm 16 nơ-ron kết nối đầy đủ, sử dụng hàm kích hoạt \textit{ReLU} để học các biểu diễn phi tuyến tính sâu hơn. Số lượng tham số là: $32 \times 16 + 16 = 528$.
    \item \textbf{Lớp Dense đầu ra (Output Layer):} Gồm \textbf{24 nơ-ron đầu ra} ứng với 24 giờ tiếp theo ($t+1$ đến $t+24$) với kích hoạt tuyến tính (\textit{Linear}) để đưa ra vector dự đoán nồng độ $PM_{2.5}$. Số lượng tham số là: $16 \times 24 + 24 = 408$.
\end{itemize}

Tổng số tham số có thể huấn luyện (\textit{Trainable parameters}) của mạng LSTM đa bước là \textbf{31,528} tham số.

Sơ đồ kiến trúc mạng LSTM chi tiết được mô tả trong Hình~\ref{fig:lstm-arch}.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=0.45cm,
        lstmblock/.style={rectangle, rounded corners=2pt, minimum width=4.6cm, minimum height=0.78cm, align=center, draw=blue!75!black, fill=blue!6, font=\small},
        dropblock/.style={rectangle, rounded corners=2pt, minimum width=4.6cm, minimum height=0.78cm, align=center, draw=orange!85!black, fill=orange!8, font=\small},
        denseblock/.style={rectangle, rounded corners=2pt, minimum width=4.6cm, minimum height=0.78cm, align=center, draw=green!55!black, fill=green!8, font=\small},
        detail/.style={text width=6.8cm, align=left, font=\footnotesize},
        flow/.style={thick,->,>=stealth}
    ]
        \node[lstmblock] (input) {Input sequence\\$(48, 6)$};
        \node[lstmblock, below=of input] (lstm1) {LSTM layer 1\\64 units};
        \node[dropblock, below=of lstm1] (drop1) {Dropout\\rate = 0.2};
        \node[lstmblock, below=of drop1] (lstm2) {LSTM layer 2\\32 units};
        \node[dropblock, below=of lstm2] (drop2) {Dropout\\rate = 0.2};
        \node[denseblock, below=of drop2] (dense1) {Dense\\16 units, ReLU};
        \node[denseblock, below=of dense1] (output) {Output\\24 PM2.5 values};

        \node[detail, right=0.9cm of input] {Cửa sổ quan sát 48 giờ, gồm 6 đặc trưng: $PM_{2.5}$, nhiệt độ, độ ẩm, gió, áp suất, $PM_{10}$.};
        \node[detail, right=0.9cm of lstm1] {Output: $(48, 64)$; activation $tanh$, gates $sigmoid$; params: 18,176.};
        \node[detail, right=0.9cm of drop1] {Giảm đồng thích nghi giữa các trạng thái ẩn, chống overfitting.};
        \node[detail, right=0.9cm of lstm2] {Output: $(32)$; activation $tanh$, gates $sigmoid$; params: 12,416.};
        \node[detail, right=0.9cm of drop2] {Dropout tiếp tục regularization trước lớp fully connected.};
        \node[detail, right=0.9cm of dense1] {Biểu diễn phi tuyến cuối; params: 528.};
        \node[detail, right=0.9cm of output] {Vector dự báo $PM_{2.5}$ cho 24 giờ tiếp theo; linear activation; params: 408.};

        \draw[flow] (input) -- (lstm1);
        \draw[flow] (lstm1) -- (drop1);
        \draw[flow] (drop1) -- (lstm2);
        \draw[flow] (lstm2) -- (drop2);
        \draw[flow] (drop2) -- (dense1);
        \draw[flow] (dense1) -- (output);
    \end{tikzpicture}
    \caption{Sơ đồ kiến trúc mạng tuần tự LSTM dự báo đa bước $PM_{2.5}$ cho 24 giờ tới}
    \label{fig:lstm-arch}
\end{figure}

% ============================================================
% 4. CẤU HÌNH HUẤN LUYỆN
% ============================================================
\section{Cấu hình huấn luyện}

\subsection{Hàm mất mát và Thuật toán tối ưu}

Vì biến mục tiêu là giá trị liên tục $PM_{2.5}$, tiêu chí đánh giá chính của toàn bộ hệ thống là sai số hồi quy. MSE/RMSE được ưu tiên vì phạt mạnh các dự báo sai lớn tại các đỉnh ô nhiễm, trong khi MAE được dùng bổ sung để diễn giải sai số trung bình theo đơn vị $\mu g/m^3$.

\begin{table}[H]
    \centering
    \caption{Cấu hình hàm mục tiêu và thuật toán tối ưu của các mô hình}
    \label{tab:loss-optimizer}
    \begin{tabularx}{\textwidth}{l >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X}
        \toprule
        \textbf{Mô hình} & \textbf{Hàm mục tiêu / Loss} & \textbf{Thuật toán tối ưu} & \textbf{Learning rate / Scheduler} \\
        \midrule
        \textbf{SARIMAX} & Ước lượng hợp lý cực đại (Maximum Likelihood); đánh giá bằng RMSE/MAE. & Không dùng SGD/Adam/RMSprop; tham số được tối ưu số bằng MLE trong \texttt{statsmodels}, giới hạn \texttt{maxiter=40}. & Không áp dụng learning rate hay scheduler vì không huấn luyện theo epoch/minibatch. \\
        \addlinespace
        \textbf{XGBoost} & \texttt{reg:squarederror}, tương ứng MSE cho bài toán hồi quy. & Không dùng SGD/Adam/RMSprop; tối ưu bằng gradient boosting trên cây quyết định hồi quy. & \texttt{learning\_rate=0.05} cố định; không dùng LR scheduler; \texttt{early\_stopping\_rounds=30} để chọn số vòng boosting phù hợp. \\
        \addlinespace
        \textbf{LSTM} & \texttt{MSE loss}; metric theo dõi thêm là MAE. & Adam optimizer. & Learning rate ban đầu $0.001$; \texttt{ReduceLROnPlateau} giảm một nửa khi \texttt{val\_loss} không cải thiện sau 3 epoch. \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Siêu tham số và Phương pháp tinh chỉnh}

Cấu hình siêu tham số cuối cùng được lựa chọn dựa trên hiệu năng trên tập Validation và cơ chế dừng sớm được thể hiện trong Bảng~\ref{tab:hyperparams}.

\begin{table}[H]
    \centering
    \caption{Bảng cấu hình siêu tham số của các mô hình}
    \label{tab:hyperparams}
    \begin{tabularx}{\textwidth}{l >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X}
        \toprule
        \textbf{Mô hình} & \textbf{Siêu tham số chính} & \textbf{Phương pháp tinh chỉnh} \\
        \midrule
        \textbf{SARIMAX} & Order: $(1, 0, 1)$ \newline Seasonal Order: $(1, 0, 0, 24)$ \newline \texttt{maxiter=40}; biến ngoại sinh gồm khí tượng và độ trễ $PM_{2.5}$ & Thử nghiệm thủ công trên tập validation \\
        \addlinespace
        \textbf{XGBoost} & $n\_estimators = 500$ \newline $max\_depth = 6$ \newline $learning\_rate = 0.05$ \newline $subsample = 0.8$ \newline $colsample\_bytree = 0.8$ \newline $min\_child\_weight = 3$ \newline $reg\_lambda = 1$, $reg\_alpha = 0$ & Randomized Search và tinh chỉnh thủ công trên Validation; \texttt{early\_stopping\_rounds=30} \\
        \addlinespace
        \textbf{LSTM} & $batch\_size = 256$ \newline $epochs = 12$ \newline $seq\_len = 48$ \newline $pred\_len = 24$ \newline $dropout = 0.2$ \newline $learning\_rate = 0.001$ & Thử nghiệm thủ công trên Validation; \texttt{EarlyStopping(patience=5)} và \texttt{ReduceLROnPlateau(patience=3)} \\
        \bottomrule
    \end{tabularx}
\end{table}

Để ngăn chặn hiện tượng quá khớp, mạng LSTM sử dụng callback \texttt{EarlyStopping} với tham số \texttt{patience=5} (dừng học nếu sai số kiểm định không cải thiện liên tiếp 5 epoch) và tự động khôi phục lại bộ trọng số tốt nhất (\texttt{restore\_best\_weights=True}).

% ============================================================
% 5. KẾT QUẢ THỰC NGHIỆM
% ============================================================
\section{Kết quả thực nghiệm}

\subsection{Quy ước đánh giá và lựa chọn mô hình}

Ba mô hình được đánh giá thống nhất trên cùng bài toán hồi quy dự báo $PM_{2.5}$ trước 24 giờ bằng RMSE và MAE trên tập Test. Sau khi so sánh định lượng, XGBoost được chọn làm mô hình chính để phân tích sâu vì đạt sai số Test thấp nhất. Do đó, các phần ma trận nhầm lẫn, chỉ số cảnh báo hậu xử lý và ví dụ lỗi cụ thể tập trung vào XGBoost. SARIMAX vẫn được trình bày như baseline thống kê, còn LSTM được phân tích riêng về kiến trúc và learning curve vì đây là mô hình học sâu của hệ thống.

\subsection{Biểu đồ quá trình học (Learning Curves)}

Quá trình huấn luyện các mô hình học máy (LSTM và XGBoost) được giám sát chặt chẽ qua diễn biến sai số trên tập Train và tập Validation. Quá trình hội tụ được mô tả chi tiết trong Hình~\ref{fig:lstm-curve} và Hình~\ref{fig:xgb-curve}.

Vì đây là bài toán hồi quy, báo cáo không dùng Accuracy trong learning curve. Thay vào đó, các đường học được biểu diễn bằng MSE/MAE đối với LSTM và RMSE đối với XGBoost. Với SARIMAX, khái niệm learning curve theo epoch không áp dụng vì mô hình được ước lượng bằng MLE, không huấn luyện qua epoch như mạng nơ-ron.

\begin{table}[H]
    \centering
    \caption{Quy ước biểu diễn quá trình học của từng mô hình}
    \label{tab:learning-curve-availability}
    \begin{tabularx}{\textwidth}{l l >{\raggedright\arraybackslash}X}
        \toprule
        \textbf{Mô hình} & \textbf{Trục huấn luyện} & \textbf{Metric biểu diễn} \\
        \midrule
        \textbf{LSTM} & Epoch & Train/Validation MSE loss và MAE \\
        \textbf{XGBoost} & Boosting round & Train/Validation RMSE \\
        \textbf{SARIMAX} & Không áp dụng epoch/round & Không có learning curve theo epoch; đánh giá bằng RMSE/MAE và đồ thị dự báo trên tập Test \\
        \bottomrule
    \end{tabularx}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/lstm_learning_curves.png}
    \caption{Biểu đồ quá trình học của LSTM trên tập Train và Validation}
    \label{fig:lstm-curve}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/xgboost_learning_curve.png}
    \caption{Biểu đồ quá trình học của XGBoost trên tập Train và Validation}
    \label{fig:xgb-curve}
\end{figure}

\textbf{Nhận xét biểu đồ hội tụ:}
\begin{itemize}
    \item \textbf{Mô hình LSTM (Hình~\ref{fig:lstm-curve}):} Cả hai đường sai số trên tập Train và Validation đều giảm trong 12 epoch. Validation loss giảm từ khoảng $0.0298$ ở epoch 1 xuống $0.0137$ ở epoch 12, cho thấy mô hình có hội tụ; tuy nhiên khoảng cách giữa Train loss và Validation loss vẫn tồn tại nên cần tiếp tục kiểm soát overfitting.
    \item \textbf{Mô hình XGBoost (Hình~\ref{fig:xgb-curve}):} Sai số RMSE giảm nhanh trong khoảng 100 vòng lặp đầu tiên. Sau đó, tốc độ cải thiện trên tập Validation chậm lại và hội tụ quanh mức $18.90 \mu g/m^3$. Kết quả Test đạt $16.42 \mu g/m^3$, tốt nhất trong ba mô hình, nhưng khoảng cách giữa Train và Validation cho thấy vẫn còn hiện tượng quá khớp ở mức vừa phải.
    \item \textbf{Mô hình Baseline SARIMAX:} Không có biểu đồ quá trình học (Learning Curves). Do đây là mô hình thống kê tuyến tính cổ điển, các trọng số được ước lượng trực tiếp một lần duy nhất qua phương pháp ước lượng hợp lý cực đại (\textit{Maximum Likelihood Estimation - MLE}), không trải qua các vòng lặp huấn luyện dạng gradient descent (epoch/round) giống như các mô hình học máy và học sâu (XGBoost, LSTM).
\end{itemize}

\subsection{Đánh giá định lượng trên tập Test}

Để có ý nghĩa thực tế cao đối với người bệnh hô hấp mãn tính, hiệu năng của mô hình được đánh giá trên bài toán \textbf{dự báo trước 24 giờ}. Hiệu năng hồi quy của các mô hình trên tập kiểm thử được tóm tắt trong Bảng~\ref{tab:regression-metrics}.

\begin{table}[H]
    \centering
    \caption{Bảng so sánh hiệu năng hồi quy dự báo trước 24 giờ trên tập Test}
    \label{tab:regression-metrics}
    \begin{tabular}{llrr}
        \toprule
        \textbf{Mô hình} & \textbf{Phạm vi} & \textbf{RMSE ($\mu g/m^3$)} & \textbf{MAE ($\mu g/m^3$)} \\
        \midrule
        \textbf{SARIMAX (Baseline)} & Hà Nội & 26.03 & 19.12 \\
        \textbf{LSTM (Deep Learning)} & Ba thành phố & 20.90 & 12.56 \\
        \textbf{XGBoost (Advanced)} & Ba thành phố & \textbf{16.42} & \textbf{10.21} \\
        \bottomrule
    \end{tabular}
\end{table}

Về cách dự báo, SARIMAX dự báo đệ quy theo chuỗi thời gian với biến ngoại sinh; LSTM dự báo vector đa bước 24 giờ từ chuỗi 48 giờ trước đó; XGBoost dự báo trực tiếp $PM_{2.5}$ tại mốc $t+24$ từ đặc trưng trễ, rolling và khí tượng.

Lưu ý về phạm vi so sánh: SARIMAX được dùng như baseline thống kê trên chuỗi Hà Nội vì mô hình này phụ thuộc mạnh vào một chuỗi thời gian đơn. XGBoost và LSTM được huấn luyện trên cả ba thành phố nhờ mã hóa biến \texttt{city} và thiết kế dữ liệu theo mẫu/sequence. Do đó, Bảng~\ref{tab:regression-metrics} dùng để so sánh xu hướng hiệu năng giữa ba hướng tiếp cận; mô hình được chọn để phân tích sâu là XGBoost vì có sai số Test thấp nhất trong phạm vi huấn luyện rộng hơn.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/sarimax_test_forecast.png}
    \caption{Dự báo $PM_{2.5}$ của SARIMAX so với giá trị thực tế trên 240 giờ đầu của tập Test}
    \label{fig:sarimax-test-forecast}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/xgboost_test_forecast.png}
    \caption{Dự báo $PM_{2.5}$ của XGBoost so với giá trị thực tế trên 240 thời điểm đầu của Hà Nội trong tập Test}
    \label{fig:xgb-test-forecast}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/lstm_test_forecast.png}
    \caption{Dự báo $PM_{2.5}$ của LSTM so với giá trị thực tế trên 240 thời điểm dự báo đầu của Hà Nội trong tập Test}
    \label{fig:lstm-test-forecast}
\end{figure}

Ba hình dự báo được đặt trên cùng thành phố Hà Nội để so sánh trực quan với baseline SARIMAX. Các chỉ số RMSE/MAE trong Bảng~\ref{tab:regression-metrics} vẫn được hiểu theo phạm vi đã ghi trong cột ``Phạm vi''.

\textbf{Nhận xét kết quả:}
\begin{itemize}
    \item Sai số dự báo trước 24 giờ cao hơn đáng kể so với dự báo ngắn hạn do hiện tượng tích lũy sai số chuỗi (\textit{error accumulation}) và các biến động ô nhiễm đột ngột. XGBoost đạt MAE $10.21 \mu g/m^3$, là kết quả tốt nhất trong nhóm mô hình đã thử nghiệm.
    \item LSTM đạt MAE $12.56 \mu g/m^3$, thấp hơn SARIMAX nhưng chưa vượt XGBoost. Điều này cho thấy mạng tuần tự học được xu hướng thời gian, nhưng cần thêm tuning/feature khí tượng tương lai để cải thiện dự báo đỉnh ô nhiễm.
    \item XGBoost giữ vị thế dẫn đầu nhờ khai thác hiệu quả đặc trưng trễ, rolling và biến ngoại sinh dạng bảng.
\end{itemize}

\subsection{Ma trận nhầm lẫn sau khi phân nhóm PM2.5 dự báo}

Biến mục tiêu của mô hình vẫn là giá trị liên tục $PM_{2.5}$ sau 24 giờ, do đó mô hình được huấn luyện như một bài toán hồi quy. Để phục vụ yêu cầu phân tích cảnh báo, $PM_{2.5}$ thực tế và $PM_{2.5}$ dự báo được quy đổi hậu xử lý sang 6 nhóm rủi ro theo ngưỡng PM2.5. Bảng~\ref{tab:confusion-matrix} trình bày ma trận nhầm lẫn của mô hình XGBoost trên tập Test sau bước ánh xạ này.

\begin{table}[H]
    \centering
    \caption{Ma trận nhầm lẫn sau khi ánh xạ $PM_{2.5}$ thực tế/dự báo sang nhóm rủi ro (XGBoost; đơn vị: số mẫu)}
    \label{tab:confusion-matrix}
    \begin{tabular}{c|cccccc}
        \toprule
        \textbf{Thực tế $\backslash$ Dự đoán} & \textbf{Tốt} & \textbf{Trung bình} & \textbf{Kém} & \textbf{Xấu} & \textbf{Rất xấu} & \textbf{Nguy hại} \\
        \midrule
        \textbf{Tốt} & \textbf{147} & 1493 & 77 & 7 & 0 & 0 \\
        \textbf{Trung bình} & 35 & \textbf{7738} & 1037 & 118 & 0 & 0 \\
        \textbf{Kém} & 1 & 926 & \textbf{1044} & 275 & 0 & 0 \\
        \textbf{Xấu} & 0 & 189 & 794 & \textbf{891} & 0 & 0 \\
        \textbf{Rất xấu} & 0 & 0 & 13 & 32 & \textbf{0} & 0 \\
        \textbf{Nguy hại} & 0 & 0 & 0 & 0 & 0 & \textbf{0} \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/xgboost_confusion_matrix.png}
    \caption{Ma trận nhầm lẫn dạng heatmap của XGBoost sau khi ánh xạ $PM_{2.5}$ thực tế/dự báo sang nhóm rủi ro}
    \label{fig:xgb-confusion-matrix}
\end{figure}

Các chỉ số Accuracy, Precision, Recall và F1-Score trong Bảng~\ref{tab:classification-metrics} vì vậy là chỉ số của lớp cảnh báo hậu xử lý, không phải hàm mục tiêu huấn luyện của mô hình.

\begin{table}[H]
    \centering
    \caption{Các chỉ số cảnh báo sau khi phân nhóm $PM_{2.5}$ trước 24h (đơn vị: \%)}
    \label{tab:classification-metrics}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Chỉ số} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} \\
        \midrule
        \textbf{Giá trị (\%)} & 66.28\% & 68.27\% & 66.28\% & 63.30\% \\
        \bottomrule
    \end{tabular}
\end{table}

\textbf{Phân tích ma trận nhầm lẫn:}
Mặc dù khoảng cách dự báo tăng lên 24 giờ, độ chính xác tổng thể của hệ thống đạt \textbf{66.28\%}, F1-Score có trọng số đạt \textbf{63.30\%}. Mô hình phân biệt tốt nhất ở nhóm \textit{Trung bình}, nhưng còn nhầm lẫn đáng kể giữa các nhóm liền kề như \textit{Kém} và \textit{Xấu}. Hai lớp \textit{Rất xấu} và \textit{Nguy hại} rất ít mẫu trong tập Test nên mô hình chưa nhận diện ổn định; đây là điểm cần cải thiện bằng cân bằng dữ liệu hoặc tối ưu hàm mất mát theo chi phí cảnh báo.

% ============================================================
% 6. THẢO LUẬN VÀ PHÂN TÍCH LỖI
% ============================================================
\section{Thảo luận \& Phân tích lỗi}

\subsection{Hiện tượng Overfitting và Underfitting}

Dựa vào sai số trên tập Train, Validation và Test của mô hình XGBoost:
\begin{itemize}
    \item \textbf{Train RMSE:} $11.55 \mu g/m^3$
    \item \textbf{Validation RMSE:} $18.90 \mu g/m^3$
    \item \textbf{Test RMSE:} $16.42 \mu g/m^3$
\end{itemize}

\begin{table}[H]
    \centering
    \caption{Chẩn đoán overfitting/underfitting dựa trên RMSE Train/Validation/Test (đơn vị: $\mu g/m^3$)}
    \label{tab:overfit-diagnosis}
    \begin{tabularx}{\textwidth}{@{}lccc>{\raggedright\arraybackslash}X@{}}
        \toprule
        \textbf{Mô hình} & \shortstack{\textbf{Train}\\\textbf{RMSE}} & \shortstack{\textbf{Validation}\\\textbf{RMSE}} & \shortstack{\textbf{Test}\\\textbf{RMSE}} & \textbf{Kết luận} \\
        \midrule
        \textbf{XGBoost} & 11.55 & 18.90 & 16.42 & Có overfitting mức vừa phải nhưng tổng quát hóa tốt nhất trong ba mô hình. \\
        \textbf{LSTM} & 16.69 & 24.64 & 20.90 & Có overfitting rõ hơn XGBoost; cần Dropout, Early Stopping và thêm đặc trưng tương lai. \\
        \textbf{SARIMAX} & 4.72 & 26.40 & 26.03 & Khả năng tổng quát hóa kém do giả định tuyến tính và lệch phân phối theo thời gian. \\
        \bottomrule
    \end{tabularx}
\end{table}

Sự chênh lệch giữa sai số huấn luyện và sai số kiểm định là khoảng $7.35 \mu g/m^3$, cho thấy mô hình có dấu hiệu quá khớp ở mức vừa phải. Tuy nhiên, XGBoost vẫn là mô hình tốt nhất trên tập Test trong ba mô hình so sánh. Mạng LSTM cũng có khoảng cách Train--Validation rõ rệt (RMSE $16.69$ so với $24.64 \mu g/m^3$), nên cần Early Stopping và Dropout để hạn chế học thuộc nhiễu trong chuỗi. Các biện pháp kiểm soát quá khớp đã áp dụng gồm:
\begin{enumerate}
    \item Áp dụng cơ chế Regularization $L_1$/$L_2$ tích hợp trong thuật toán cây của XGBoost.
    \item Sử dụng tỷ lệ lấy mẫu con của dòng và cột (\texttt{subsample} = 0.8, \texttt{colsample\_bytree} = 0.8) để bổ sung tính ngẫu nhiên tương tự Random Forest.
    \item Cơ chế dừng sớm tự động ngắt khi sai số trên tập Validation đi ngang.
\end{enumerate}

Mô hình SARIMAX có Train RMSE thấp ($4.72 \mu g/m^3$) nhưng Validation/Test RMSE tăng mạnh ($26.40$ và $26.03 \mu g/m^3$). Do đó, vấn đề chính không phải là underfitting thuần túy, mà là khả năng tổng quát hóa kém khi chuỗi kiểm thử có phân phối và đỉnh ô nhiễm khác giai đoạn huấn luyện. Giới hạn này xuất phát từ giả định tuyến tính của SARIMAX và việc mô hình khó biểu diễn tương tác phi tuyến giữa khí tượng, độ trễ và nguồn phát thải.

\subsection{Phân tích các trường hợp dự đoán sai điển hình}

Qua rà soát chi tiết các mẫu dự đoán trước 24h có sai số tuyệt đối lớn nhất ($Error > 30 \mu g/m^3$), nhóm ghi nhận một số trường hợp sai điển hình trong Bảng~\ref{tab:error-cases} và phát hiện ba kịch bản lỗi hệ thống sau:

\begin{table}[H]
    \centering
    \scriptsize
    \caption{Một số trường hợp dự đoán sai lớn nhất của XGBoost trên tập Test (đơn vị $PM_{2.5}$: $\mu g/m^3$)}
    \label{tab:error-cases}
    \begin{tabular}{p{2.4cm}p{1.3cm}rrrp{2.0cm}p{4.2cm}}
        \toprule
        \textbf{Thời điểm dự báo} & \textbf{Thành phố} & \textbf{Thực tế} & \textbf{Dự báo} & \textbf{Sai số} & \textbf{Lớp thực tế/dự báo} & \textbf{Giả thuyết nguyên nhân} \\
        \midrule
        2026-04-25 06:00 & Hà Nội & 186.70 & 45.76 & 140.94 & Rất xấu/Kém & Đánh giá thấp đỉnh ô nhiễm, có thể thiếu biến nguồn thải đột xuất. \\
        2026-04-25 05:00 & Hà Nội & 181.60 & 47.28 & 134.32 & Rất xấu/Kém & Độ ẩm rất cao và biến động cục bộ làm mô hình dự báo thấp hơn thực tế. \\
        2026-04-12 19:00 & Hà Nội & 185.30 & 55.92 & 129.38 & Rất xấu/Xấu & Đỉnh ô nhiễm buổi tối không được đặc trưng trễ và khí tượng giải thích đầy đủ. \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{enumerate}
    \item \textbf{Sự suy giảm hiệu năng theo khoảng thời gian dự báo (Horizon Decay \& Error Accumulation):} Khi dự đoán xa đến 24h hoặc 48h, sự thiếu hụt các thông tin tức thời làm tích lũy sai số. Mô hình dựa nhiều vào dự báo khí tượng ngoại sinh tương lai làm đầu vào phụ trợ, do đó sự sai lệch của mô hình dự báo thời tiết cũng sẽ trực tiếp phóng đại sai số dự đoán bụi mịn.
    \item \textbf{Trễ nhịp tại các điểm uốn đột biến cực đoan (Peak underestimation):} Với tầm dự báo xa 24h, các đợt phát thải đột xuất cục bộ (như sự kiện đốt pháo hoa đêm giao thừa, hoạt động đốt rơm rạ theo giờ bất chợt ở ngoại thành) gần như không thể dự đoán được trước 1 ngày, dẫn đến mô hình thường đánh giá thấp các đỉnh ô nhiễm này.
    \item \textbf{Thay đổi thời tiết đột ngột (Sudden Weather Transitions):} Sự xuất hiện ngoài kế hoạch của các cơn mưa rào lớn ở miền Nam hoặc các đợt gió mùa Đông Bắc tràn về sớm làm thay đổi hoàn toàn cơ chế đối lưu không khí trước 24h, khiến mô hình đưa ra dự báo quá cao (\textit{Overestimation}) do chưa kịp cập nhật thông tin khí tượng tức thời.
\end{enumerate}

% ============================================================
% 7. TÀI LIỆU THAM KHẢO
% ============================================================
\begin{thebibliography}{99}
\bibitem{xgboost} Chen, T., \& Guestrin, C. (2016). XGBoost: A Scalable Tree Boosting System. In \textit{Proceedings of the 22nd ACM SIGKDD International Conference on Knowledge Discovery and Data Mining} (pp. 785-794).
\bibitem{lstm} Hochreiter, S., \& Schmidhuber, J. (1997). Long Short-Term Memory. \textit{Neural Computation}, 9(8), 1735-1780.
\bibitem{statsmodels} Seabold, S., \& Perktold, J. (2010). Statsmodels: Econometric and statistical modeling with python. In \textit{Proceedings of the 9th Python in Science Conference} (Vol. 57, pp. 61-68).
\end{thebibliography}

\end{document}
